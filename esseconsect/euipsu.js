// Defining an array named 'signals'
let signals = [];

// Defining a signal to be added to the array
let signal = "newSignal";

// Pushing the signal to the 'signals' array
signals.push(signal);

// Printing the array to verify the result
console.log(signals);
